import React, { useState } from 'react';
import { GridItem, Button } from '@chakra-ui/react';

const GridContentSwitcher = () => {
  const [displayedContent, setDisplayedContent] = useState('default');

  const switchContent = (content) => {
    // Update the displayed content based on the current state
    setDisplayedContent((content) => {

      if (content === 'import'){

      }
    }
    );
  };
};